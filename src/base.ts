import { Scene } from './scene';
import { Camera } from './camera';
import { Renderer } from './renderer';

export const scene = Scene.getScene();
export const camera = Camera.getCamera();
export const renderer = Renderer.getRenderer();
